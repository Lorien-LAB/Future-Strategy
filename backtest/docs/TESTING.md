# 测试与验证证据

## 执行命令

```bash
python -m pip install -e '.[dev,charts]'
python -m compileall -q backtest
python -m pytest -q --junitxml=test-results.xml
python -m backtest demo --output backtest/demo_output --run
```

GitHub工作流 `.github/workflows/backtest.yml` 在Python3.11/3.13运行以上验证，并检查生成净值图、回撤图、非零实际入场数。报告和JUnit作为工作流artifact保存14天。工作流存在不等于已通过；请绑定具体提交检查。

## 已取得的验证

核心代码提交 `c9cac06d829e648480f8108adf5cc5a1d0336730` 在工作流 `35303180827` 完成两个版本的全部步骤。Python3.11日志确认 **73 passed**，合成CLI在0/1/2bp每个情景的正式演示窗口均产生16笔入场，而不是空交易流程假通过。Python3.13任务也为success。

该记录只证明对应提交上的软件/合成集成测试；后续任何代码改变需要重新跑。不能将历史日志当成后续版本测试证明。详细日志由GitHub Actions保留。

## 覆盖的行为

- 配置：枚举、布尔、finite、网格规模、未知配置拒绝、显式回顾路由。
- 特征：严格左窗口、总体标准差、VR部分窗口及窗长、0波动分子、CL零振幅、F25缺失与可见性。
- 状态：追加未来不改变过去；OPEN/订单保留；跨年原模型不被替换；缺退出价格待执行；同日最终OI不反向取消开盘；代理与延后执行差异；shadow/actual。
- 账本：固定数量望远镜PnL、entry费用与滑点时点、非出资旧仓不再投资、最大仓出资、整数手和成本预算、旧仓底线、无新成交不调旧仓。
- 数据：日期编码、实际合约/月序列边界、重复行拒绝、有效open保留、PIT规格和版本生效、Parquet。
- 产物：源码/配置/历史前缀变化拒绝模型复用；失败留证；不覆盖结果；比较首个事件差异；端到端校准/成本门/费用/净值。

## 未完成的实证验证

没有提供原完整Parquet快照和历史规格，因此没有运行原市场全历史重放、没有验证修正后是否仍有43.08%年化、没有计算旧新逐笔差异数量、没有验证盘口成交容量。73项测试不是形式化证明，也不覆盖所有数据供应商、交易所规则与极端市场。

## 研究运行前的验收

以小范围真实数据先跑validate；固定数据与规格哈希，检查早期训练数据足够；人工核对几笔双腿方向、开盘和结算损益、费用与手数；检查全部缺失/拒绝/OPEN状态；再运行完整年度网格。真实输入失败时优先修复数据契约或策略假设，不通过降低门槛、替换收益基准或删掉异常交易来获得漂亮结果。
