# Agent 接手规范

## 唯一运行入口与阅读顺序

本仓库只维护一份正式回测实现：根目录 `backtest/`。先读 `backtest/README.md`、`backtest/docs/MIGRATION.md`、`backtest/docs/STRATEGY.md`，再读 `backtest/pipeline.py`、`backtest/engine.py`。用 `python -m backtest demo --output <新目录> --run` 验证环境，不需要凭空制造真实行情。

## 模块边界

`data.py` 是市场文件读取入口；`domain.py` 定义数据结构；`features.py` 计算纯特征；`gates.py` 只消费 Signal 和当前开盘，不得接收完整交易结果；`strategy.py` 更新退出状态；`ledger.py` 是唯一损益和数量真相；`allocation.py` 只给出数量调整计划；`engine.py` 执行时间线；`training.py` 只用部署前数据选模型；`reporting.py` 只导出同一账本。

禁止新增 `v2_final/`、`optimized/` 或 notebook 内完整回测副本。实验通过配置和纯函数进入正式接口。不要让报告重新推导另一套净值。

## 不可破坏的约束

1. 追加未来数据不能改变既有模型下过去的信号、成交、费用和持仓；未来只能新增后来事件。
2. 年末更新模型不清除旧仓；旧仓继续使用原信号的合约、中心和参数，直到真实退出。
3. 有效开盘不依赖稍后的收盘、结算、全天 OI/成交量。后续缺价失败时，过去成交仍保留。
4. 结算现金流不会自动改变手数。任何数量变化都有 fill 和费用；无融资需要时，cash-sweep 不调整非出资旧仓。
5. 正常回测不读冻结收益/退出标签做准入，不要求匹配旧 43.0781% 年化。
6. 经验品种名单、F25 注册表、追溯规格和 same_open_proxy 必须在 manifest 明确标注。不得宣传为独立未来验证。
7. 不猜合约乘数、保证金、交易时间或真实数据。未知规格失败关闭。
8. 不覆盖完整运行目录；失败产物标记 FAILED 并保留账本，不静默丢弃异常交易。
9. 不修改原 Future-Spread-Trader、live、账户或模拟盘；这里是研究环境，没有下单接口。

## 修改流程

修一个问题先加可失败测试；代码修改后运行 `python -m pytest -q` 和非空合成全流程。模型包绑定源码、校准配置和历史数据前缀；代码改变后需要重新校准，不手工伪造 hash 通过检查。真实新实验固定配置并保存数据/规格快照，不覆盖或重新定义旧结果。

提交说明必须写明：机械调整还是策略规则变化、修改哪些配置/接口、测试实际结果、是否跑过真实数据、哪些限制尚未解决。测试绿色不等于交易可执行或未来盈利。

## 未实现能力

不支持分钟/逐笔撮合、夜盘时间戳调度、部分成交/腿间风险、账户强平策略、断点状态恢复、真实历史逐品种费率引擎。`state.json` 是诊断状态而不是可恢复 checkpoint；重启采用完整前缀重放。不要在文档中把这些能力写成已有功能。
